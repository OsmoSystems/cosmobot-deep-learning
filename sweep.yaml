# TODO will these sweeps be checked in? where do these sweep configurations live? how are thye named?
# TODO real description
description: a Simple CNN sweep

program: cosmobot_deep_learning/models/simple_cnn.py

method: bayes

metric:
  name: val_mean_absolute_error
  goal: minimize

# TODO understand early termination methods and decide whether we should try it out

parameters:
  epochs:
    value: 200
  dataset-cache:
    value: sweep
  learning-rate:
    min: 0.0001
    max: 0.003
